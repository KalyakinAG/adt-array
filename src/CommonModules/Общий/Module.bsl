Функция АТДМассив(Элементы = Неопределено) Экспорт
	АТДМассив = Обработки.АТДМассив.Создать();
	Если Элементы <> Неопределено Тогда
		АТДМассив.Элементы = Элементы;
	КонецЕсли;
	Возврат АТДМассив;
КонецФункции
